# LaraVuetify

Laravel Vuetify 

## Installation



```cmd
cd laravel-vue
```

Install php dependencies

```cmd
composer install
```

Install js dependencies

```cmd
npm install && npm run watch
```

Run migrations

```cmd
php artisan migrate
```

Running the server

```cmd
php artisan serve
```